# Hundslachs - `getFieldProps()`

+ With the `useFormik()` hook and `getFieldProps(field_name)` you can get the specify every inpput area. 

+ formik tutorial: href="https://jaredpalmer.com/formik/docs/tutorial"